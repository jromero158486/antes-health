
const navToggle=document.querySelector('.nav-toggle');const navLinks=document.querySelector('.nav-links');
if(navToggle&&navLinks){navToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});navLinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navLinks.classList.remove('open');navToggle.setAttribute('aria-expanded','false');}));}
const file=(location.pathname.split('/').pop()||'index.html');document.querySelectorAll('.nav-links a').forEach(a=>{const href=a.getAttribute('href');if(href===file)a.classList.add('active');});
const prefersReduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const revealEls=document.querySelectorAll('.reveal');
if(!prefersReduced&&'IntersectionObserver'in window){const obs=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in-view');obs.unobserve(e.target);}})},{threshold:.12});revealEls.forEach(el=>obs.observe(el));}else{revealEls.forEach(el=>el.classList.add('in-view'));}
const flow=document.querySelector('.data-flow');const tip=document.querySelector('.tooltip');const nodes=document.querySelectorAll('.flow-node');
if(flow&&tip&&nodes.length){nodes.forEach(node=>{const show=(event)=>{const text=node.dataset.tip;if(!text)return;tip.textContent=text;tip.style.display='block';const rect=flow.getBoundingClientRect();let x=(event.clientX||rect.left+40)-rect.left+14;let y=(event.clientY||rect.top+40)-rect.top+14;x=Math.min(Math.max(12,x),rect.width-280);y=Math.min(Math.max(12,y),rect.height-80);tip.style.left=x+'px';tip.style.top=y+'px';};const hide=()=>{tip.style.display='none';tip.textContent='';};node.addEventListener('mousemove',show);node.addEventListener('mouseenter',show);node.addEventListener('mouseleave',hide);node.addEventListener('focus',show);node.addEventListener('blur',hide);});}
